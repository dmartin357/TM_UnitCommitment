Alright now let me describe what I'd like to do for the stage 2 genetic algorithm.

I want to start from time period 0-1 in the sense that we have a starting point because we know the commitment status of generators at time 0. Then I want to progress from 0-1 all the way until 47-48.

The genetic algorithm portion will start by selecting chromosomes from the two time periods (in the case of time period 0 therer will only be one candidate so the selection is trivial). I would like the probabity of selection for the first generation to be uniform to start even though I would like to leave the possibility to specify a different distribution in the future to determine if there is one that works better. Once candidates are selected we need to process some information about them, namely determining the number of startups and shutdowns that occured the pair represents. Another part of processing by rhe pair will be to determine if any minimum up or minimum down time constraints have occured, to do this we will need a way to track how long a given unit has been in its current respective state in time period t, then determine if the actions taken by the chromosome in time period t+1 violate minimum up and down time constraints, if so, I want to correct these by switching the commitment status in time period t+1 (not in the actual t+1 but for the purposes of "mating" or the pairing we're doing). After this happens, I want to check startups and shutdowns againt out targets. Then, I want to mutate the set of units in time period t+1 that are eligible to be startup or shutdown until either our target goals are reached or we are unable to startup or shutdown any more units. After we stop there, I woud like to rerun a two period economic dispatch problem, to do this I'm just envisioning double the constraints to handle time period t and time period t+1. This is what I'm imagining will enforce ramping constraints. I guess time period t is known. so in reality this will just be regular ED for time period t+1.

I wnant to perform a certain number of these combinations randomly selected. And then choose the cheapest pairing as our winner. Then use that as the new t in the next time period iteration.

Now that I say it out loud, I'm not sure if this is a classic GA or not, but either way I think an interesting algorithm. Does that make sense?
